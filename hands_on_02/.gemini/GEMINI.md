# AGENTS.md — Root Orchestrator Instructions

> **IMMUTABILITY CONSTRAINT (MANDATORY):** The subdirectories `claude-code/` and `gemini-cli/`
> are **read-only** from your perspective. You must **NOT** modify, delete, or create any files
> inside those subdirectories. You may read their outputs to perform your tasks.

---

## Context

This is the root of the **Hands-on 02 — CAP 417 — Part C** project.

Two AI agents have independently produced solutions to the assignment inside:
- `claude-code/` — solution generated by Claude Code
- `gemini-cli/` — solution generated by Gemini CLI

Your role is **not** to re-implement the assignment. Your role is to:
1. Read and compare the two agent outputs.
2. Produce a **benchmark report** comparing them.
3. Produce a **discussion document** answering the theoretical questions from Part 2.6.
4. Assemble a **final deliverable** tying everything together.

---

## Your Tasks

### Task 1 — Benchmark Report

Compare the two agent solutions across the following dimensions.
Read both `claude-code/solution.py` and `gemini-cli/solution.py`, and any generated figures/CSVs.

Produce `benchmark/benchmark_report.md` with the following structure:

#### 1.1 Algorithmic Comparison

For each of Parts 2.1, 2.2, 2.3, compare how each agent implemented the task:

| Aspect                        | Claude Code | Gemini CLI |
|-------------------------------|-------------|------------|
| Brownian series generation    | ...         | ...        |
| Shuffle method                | ...         | ...        |
| Colored noise generation      | ...         | ...        |
| Moment computation (library)  | ...         | ...        |
| CFP plot approach             | ...         | ...        |

Highlight any meaningful algorithmic differences (e.g., FFT normalization, kurtosis convention — excess vs. raw, random seed handling).

#### 1.2 Numerical Results Comparison

Load both `results/moments_table.csv` and `results/noise_moments.csv` from each agent.
Compare:
- Do both agents agree on the statistical moments for the Brownian series (Part 2.1)?
- Compute the difference in skewness and kurtosis values for the 100 realizations per noise type. Report mean and std of the difference.
- Are the CFP scatter plots qualitatively consistent (same clustering pattern)?

Produce `benchmark/moments_comparison.csv` with columns:
`[noise_type, metric, claude_mean, claude_std, gemini_mean, gemini_std, abs_diff_mean]`

#### 1.3 Code Quality Assessment

Evaluate each solution on:
- Readability and structure
- Reproducibility (seeding)
- Plot quality (labels, legends, dark background compliance)
- Output file organization
- Correctness of the frequency-domain noise generation

Score each criterion 1–5 with justification.
Produce `benchmark/quality_scorecard.md`.

#### 1.4 Benchmark Timing

Run both `solution.py` files (read-only — copy to a temp location if needed to time them) and record wall-clock execution time.
Report in the benchmark report.

---

### Task 2 — Theoretical Discussion (Part 2.6)

Produce `discussion/discussion.md` answering the following questions with reference to the actual numerical results from both agents:

1. **Do the different noise types occupy distinct regions in the Cullen-Frey-Pearson space?**
   Use the CSV data to support your answer quantitatively (mean S² and K per class, with spread).

2. **Is there overlap between classes?**
   Describe any overlap observed in the scatter plots and what it implies.

3. **What does this indicate about the capacity of skewness and kurtosis to distinguish processes?**
   Address the discriminative power (and limitations) of these two statistics as process classifiers.

4. **Does shuffling alter the position in the Cullen-Frey space?**
   Compare the (S², K) values of the original Brownian series vs. the shuffled version.
   Explain theoretically why shuffling should or should not move the point in this space.

5. **Conceptual observations:**
   Note any discrepancies, surprising results, or open questions raised by comparing the two agent implementations.

---

### Task 3 — Final Assembly

Produce `README.md` at the root level with:
- Brief description of the project structure
- How to run each agent's solution
- Pointer to the benchmark report and discussion
- Any prerequisites (Python version, libraries)

Produce `run_all.sh` at the root level:
```bash
#!/bin/bash
# Runs both agent solutions sequentially and then the benchmark
set -e
cd claude-code && python solution.py && cd ..
cd gemini-cli  && python solution.py && cd ..
python benchmark/run_benchmark.py
```

Produce `benchmark/run_benchmark.py`: a Python script that loads both agents' CSV outputs and regenerates `benchmark/moments_comparison.csv` programmatically (so the comparison is reproducible).

---

## Expected Output Structure (Root Level)

```
./
├── AGENTS.md                        ← this file (do not modify)
├── README.md                        ← YOUR OUTPUT
├── run_all.sh                       ← YOUR OUTPUT
├── claude-code/                     ← READ ONLY
│   ├── AGENTS.md
│   ├── solution.py
│   ├── figures/
│   └── results/
├── gemini-cli/                      ← READ ONLY
│   ├── AGENTS.md
│   ├── solution.py
│   ├── figures/
│   └── results/
├── benchmark/
│   ├── run_benchmark.py             ← YOUR OUTPUT
│   ├── benchmark_report.md          ← YOUR OUTPUT
│   ├── moments_comparison.csv       ← YOUR OUTPUT
│   └── quality_scorecard.md         ← YOUR OUTPUT
└── discussion/
    └── discussion.md                ← YOUR OUTPUT
```

---

## Conventions

- All your Markdown files must use proper heading hierarchy (H1 → H2 → H3).
- All your CSV files must include a header row.
- All your Python scripts must be runnable from the root directory with `python <path>`.
- Do not re-implement Parts 2.1–2.3 from scratch. Consume the agent outputs.
- If an agent's output file is missing or malformed, document it explicitly in the benchmark report rather than fabricating data.
- Do not modify `claude-code/` or `gemini-cli/` under any circumstance.
