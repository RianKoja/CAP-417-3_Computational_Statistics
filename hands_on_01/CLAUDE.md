This is an exercise described on HandsOn01-ParteC-English.md

The variation chosen for additional experiments was to use N = 100, 1000, 10000 (various sizes of time series)

There is a fodler named claude-code where the code generated by claude is stored.
There is also a folder named gemini-cli where the code generated by gemini-cli is stored.

Create a jupyter notebook that compares the code generated by claude and gemini-cli for the same task, then complete the assignment described in HandsOn01-ParteC-English.md assuem the agentic parts by each agent are done.

use python and uv as a package manager. Especifically at this level only a jupyter notebook shall be used, and it shall present all results found by the each agent which clear labelling.
all code must be in this folder.
Don't modify nor reimplement anything already done by the agents, just use the artifacts created by them.